import { VIEWS_THEME, SITE_NAME, SYSTEM_URL } from './../CONFIG.js'

const PanelLogo = Vue.component('panellogo', {

    // propriedades vindas do elemento pai
    props: [],

    // informações de retorno para o componente
    data: function () {

        return {}
    },

    // componentes
    components: {},

    // renderização
    template: `   
   
     `

})

export default PanelLogo